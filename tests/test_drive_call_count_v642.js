'use strict';
const fs=require('fs'),path=require('path'),vm=require('vm');const root=path.resolve(__dirname,'..'),src=f=>fs.readFileSync(path.join(root,'js',f),'utf8');const assert=(c,m)=>{if(!c)throw new Error('FALHOU: '+m)};const ls={};const sb={console,Object,Array,String,Number,JSON,Math,Promise,Date,Map,Set,TextEncoder,window:null,localStorage:{getItem:k=>ls[k]??null,setItem:(k,v)=>ls[k]=String(v),removeItem:k=>delete ls[k]}};sb.window=sb;vm.createContext(sb);vm.runInContext(src('01g-drive-journal-v640.js')+'\nwindow.Journal=BorionDriveJournal640;',sb);ls['borion_journal_topology_v642_main']=JSON.stringify({sync:'sync',operations:'ops',applied:'applied',snapshots:'snap',conflicts:'conf',duplicates:{sync:[],operations:[],applied:[],snapshots:[],conflicts:[]}});let driveCalls=0;sb.GoogleDriveFS={async findChildren(){driveCalls++;throw new Error('não deveria listar no fast path')},async createFolder(){driveCalls++;}};(async()=>{const topology=await sb.Journal.discoverTopology('main');assert(topology.fastPath===true&&topology.canonicalOpsFolder.id==='ops','IDs persistidos devem reconstruir a topologia em memória');assert(driveCalls===0,'topologia normal não deve consultar o Drive');console.log('OK: topologia canônica persistida elimina redescobertas/listagens no caminho normal.');})().catch(e=>{console.error(e);process.exit(1)});
